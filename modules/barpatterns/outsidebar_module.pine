//@version=6
indicator('Outside Bar Module', overlay=true)
outside_bar = high > high[1] and low < low[1]
plotshape(outside_bar, style=shape.square, location=location.abovebar, color=color.purple, size=size.tiny)
