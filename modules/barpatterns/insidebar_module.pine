//@version=6
indicator('Inside Bar Module', overlay=true)
inside_bar = high < high[1] and low > low[1]
plotshape(inside_bar, style=shape.square, location=location.belowbar, color=color.blue, size=size.tiny)
